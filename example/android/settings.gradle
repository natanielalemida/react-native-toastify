pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenLocal()
        google()
    }
}

rootProject.name = 'toastify.example'
include ':app'

// Incluir o plugin do React Native corretamente
includeBuild('../node_modules/@react-native/gradle-plugin')
